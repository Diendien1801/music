name: music_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  just_audio: ^0.9.6
  
  audio_video_progress_bar: ^2.0.2
  rxdart: ^0.27.7

 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  provider: ^5.0.0
  shared_preferences: ^2.0.6
flutter:
  uses-material-design: true
  assets:
    - assets/img/
    - assets/img/mtpShow.png
